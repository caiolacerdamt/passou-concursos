# Dependencies
node_modules/

# Build / framework
.next/
out/
dist/
build/
.turbo/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Supabase local
.branches/
.temp/
supabase/.temp/

# Secrets / environment (NEVER commit real keys)
.env
.env.*
!.env.example

# Generated TTS comparison artifacts (regenerable, large binaries)
experiments/tts-comparacao/out/

# Configuração pessoal (a compartilhada é .claude/settings.json e CLAUDE.md)
.claude/settings.local.json
CLAUDE.local.md

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs & temp
*.log
*.tmp

# Provas oficiais e gabaritos (SPEC 09). Fonte legal e o PDF da banca (AD-003),
# mas binario de prova nao entra no git: sao centenas de KB por arquivo, mudam
# a cada lote e nao sao codigo. O operador os guarda fora do repositorio.
/provas/*.pdf
/provas/*.png
